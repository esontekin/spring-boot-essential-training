spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
logging:
  level:
    org.springframework.jdbc.datasource.init.ScriptUtils: debug