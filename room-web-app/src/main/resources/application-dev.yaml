spring:
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
  datasource:
    url: jdbc:postgresql://localhost:5432/dev
    platform: postgres
    username: postgres
    password: postgres